using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LancerScan : MonoBehaviour
{
    // Start is called before the first frame update
    private void Awake()
    {
        //Component a = gameObject.GetComponent<AstarPath>;
    }
}
